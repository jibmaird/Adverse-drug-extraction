#!/usr/bin/perl -w

use strict;

use vars qw ();

#For each sentence in training and test, extract the last layer from the trained model
#system("./extract_embed.pl");
#Compare vectors
#system("python compare_sentences.py > comp_log.txt");
#Extract top matches
#system("./top_matches.pl comp_log.txt");
